const express = require('express');
const Property = require('../models/propertyDetail');
const router = express.Router();

router.post('/property',async(req,res)=>{
         
    try{

        const slugIds = req.body;
        

    }
    catch(err){

    }
    
})

module.exports = router;


